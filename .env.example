# Copy this file to .env (same folder) and it'll work as-is with the Docker
# command from the README. Never commit the real .env file to git.

DATABASE_URL="postgresql://postgres:devpassword@localhost:5432/skystorage"
